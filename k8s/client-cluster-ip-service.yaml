apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    component: web
  ports: # cluster ip is not accessable from outside world
    - port: 3000
      targetPort: 3000
